#!/bin/bash

#PMUPDATES=$(pacman -Qu | grep -Fcv '[ignored]')
#PMUPDATES=$(apt list --upgradeable 2>/dev/null | tail -n +2 | wc -l)
#if [ $PMUPDATES = 0 ]; then
  #echo "^c#00ff00^📦$PMUPDATES^c#ffffff^"
#else
  #echo "📦$PMUPDATES"
#fi

PMUPDATES="cat $HOME/.cache/pkg.log"
if [[ $($PMUPDATES | tail -n 1) = "No updates available." ]]; then
    echo "^c#64dd17^📦^c#ffffff^"
fi
        if [[ $($PMUPDATES | grep SecAdvisories | awk '{print $1}') = "SecAdvisories:" ]]; then
            echo "^c#ff4500^📦^c#ffffff^"
else
    echo "^c#64dd17^📦^c#ffffff^"
    #echo "📦"
fi
