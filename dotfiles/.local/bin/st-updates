#!/bin/sh

#PMUPDATES=$(pacman -Qu | grep -Fcv '[ignored]')
PMUPDATES=$(apt list --upgradeable 2>/dev/null | tail -n +2 | wc -l)
if [ $PMUPDATES = 0 ]; then
  echo "^c#00ff00^📦$PMUPDATES^c#ffffff^"
else
  echo "📦$PMUPDATES"
fi
